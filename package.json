{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "dev:web": "lerna run --scope *web  dev --parallel",
    "dev:serverless": "lerna run --scope *serverless  dev --parallel",
    "build:web": "lerna run --scope *web  dev --parallel",
    "build:serverless": "lerna run --scope *serverless  dev --parallel",
    "build": "lerna run build --parallel"
  }
}
